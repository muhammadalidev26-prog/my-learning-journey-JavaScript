"use strict"

// DATA TYPES
// There are mainly 8 data types in JavaScript, a variable can contain a certain datatype but it isn't bound to any of them

let example = 189;
console.log(example);

example = "jake";
console.log(example);
// No error would occur here.

console.log("The Data types are discussed in other files.")